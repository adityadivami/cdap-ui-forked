import { makeStyles } from '@material-ui/styles';

export const useStyles = makeStyles(() => {
  return {
    labelTextStyles: {
      fontStyle: 'normal',
      fontWeight: 400,
      fontSize: 14,
      lineHeight: '150%',
      letterSpacing: '0.15px',
      color: '#5F6368',
    },
  };
});
